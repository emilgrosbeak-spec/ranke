# 燃刻 · 手机版健身系统 1.1

这是为自用设计的手机优先 PWA，不是 .ipa 安装包。无需上架 App Store，也无需购买域名。上传到 HTTPS 网站后，可以在 iPhone 上添加到主屏幕，独立打开。旧版 HTML 原文件未修改。

## 1. 上传到 Cloudflare Pages

建议第一次在电脑上操作。

1. 登录 Cloudflare，进入 Workers & Pages，创建应用，选择 Pages 的上传现有文件 / Direct Upload 入口。界面名称可能会调整。
2. 为项目起一个名字，上传本 ZIP；控制台拖放上传支持 ZIP 或文件夹。如果界面要求文件夹，先解压，再选择包含 index.html 的那个文件夹。
3. 发布后使用项目的 `https://项目名.pages.dev` 正式网址。不用另买域名，也不需要构建命令或依赖安装。
4. ZIP 解开第一层即应看到 index.html、styles.css、app.js 等，不要再包一层目录。如果打开是 404，请先检查这一点。
5. 不要把个人 JSON 备份上传到 Cloudflare。这里只上传应用文件包；个人数据在手机里导入。

Cloudflare 官方说明：[Direct Upload](https://developers.cloudflare.com/pages/get-started/direct-upload/)。文件包只做本地交付，本次尚未替你发布网站。

## 2. 在 iPhone 上安装

1. 用 Safari 打开正式 HTTPS 网址，不要通过聊天软件内置浏览器安装。
2. 点击分享（部分版本需先点击更多），选择“添加到主屏幕”。如果有“作为网页 App 打开 / Open as Web App”，将其打开，再添加。
3. 从主屏幕打开“燃刻”，在设置中等到离线状态显示“已缓存核心文件”。首次缓存时保持联网。
4. 建议先安装，再在主屏幕打开的应用里导入 JSON，避免不同浏览器或运行环境的数据隔离带来混淆。
5. 可自行开启飞行模式检查：关闭并重新打开应用，查看计划、添加测试记录后恢复网络。首次安装、分享/下载、特定 iOS 版本的行为仍需在你的真机上确认。

Apple 官方说明：[把网站变成 iPhone 网页 App](https://support.apple.com/guide/iphone/open-as-web-app-iphea86e5236/ios)。本地双击 index.html 仅用于简要预览，不能完成 HTTPS 离线安装。

## 3. 从旧版迁移

1. 在之前实际使用过的浏览器打开旧版网页，导出“全部数据”的 JSON 备份。上传的 HTML 只有程序，不包含浏览器中的个人设置。
2. 在新应用“设置 → 导入 JSON 备份”选择它。支持旧格式 `ranke-fitness-planner / schemaVersion: 1`，以及新格式 `ranke-mobile / schemaVersion: 1`。
3. 核对导入摘要，先备份新应用现有数据，再确认替换。导入是整份替换，不是合并。
4. 导入原网页备份后，前往“身体参数与计算结果”核对数值、四餐分配及食品重量。原参考资料 97 项保留稳定编号，已引用食物可继续计算；丢失的自定义食物会标明缺失，不会用其他食物替代。
5. 最老版本如果只有食品库导出、而不是完整数据备份，需先在旧版生成完整 JSON。

## 4. 主要功能

- 今日：查看当天作息并勾选、查看今天选用的食谱、开始当日训练。
- 作息：周一至周日分别编辑；跨夜显示次日；复制到其他日期；一周备注。
- 食谱：训练日 / 休息日独立。早餐、午餐、晚餐、练前餐（休息日可当加餐）。每餐碳水和蛋白质组各选一套方案；方案内多种食物共同构成一餐，多个方案互相替换。各餐比例可以不足或超过 100%，仍按填写比例直接计算；也可设置鸡蛋等按份食物的固定数量。
- 食品库：原资料 97 项可直接配餐，搜索并按营养类型筛选；可创建自定义食物，或复制参考食物后补全标签。
- 力量训练：四分化 / 五分化独立保存。填写每一练的名称、部位、备注；动作支持排序、组数、次数范围、重量、kg/lb/自重、组间休息和备注。每周可自行分配训练日。
- 训练中：开始时生成独立计划快照；每组记录实际次数和重量，再勾选完成；完成后可启动组间休息。刷新后可继续；结束时保存历史。中途修改计划不修改已开始的记录。
- 历史：查看逐组实际记录和训练备注。训练量仅累计已完成、次数和重量均为单一数字的 kg/lb 组，自重及次数范围不计入量。
- 宽版图片：作息、食谱分开生成，1440px 双栏 PNG。食谱图只导出“今日选用”方案；内容特别多时请直接使用应用。图片只是可选功能，不是手机使用的前提。

## 5. 营养和训练数据的边界

手机版不区分计算模式，统一沿用原网页公式：

`BMR = 体重×9.99 + 身高×6.25 − 年龄×4.92 + 性别参数（男5，女−161）`

`平衡 = BMR / 0.7 + 有氧热量 +（训练日额外力量热量）`

`目标热量 = 平衡 × 0.64`

脂肪为体重≥120kg 时70g，否则60g；蛋白质为体重×所填系数后向下取整；余下热量分配碳水。训练日与休息日的全天目标，再按各餐填写比例直接计算；两列合计不必等于100%，不足部分保持未分配，超过部分会使各餐合计高于全天目标。这个较大热量缺口来自原网页公式，并非本应用另行提出的建议。首次设置的表单数字是占位，不是对你的身体状况的推断。

参考资料中的 GI、食物说明和饮食观点来自旧版已录入的图片资料，未经本次独立核验。很多食物只列出一种营养素，缺失值不代表真实为零；显示的“已知部分”不等于完整热量或实际摄入。品牌及生熟状态有差异，实际包装优先。

用户指定备注始终保留：“若早餐不吃鸡蛋，或午餐、晚餐食用低油／无油菜，则全天补吃30g坚果。”坚果、蔬菜、烹调油等未自动加入营养计算，完成勾选仅作提醒。

应用不推荐训练负荷或动作，不是医疗、减重或营养建议。组间计时使用目标时间计算，恢复前台会显示正确剩余时间；不保证锁屏/后台响铃，也没有后台推送。

## 6. 数据、安全和更新

- 无账号、无云同步。数据通过 localStorage 保存在当前设备、当前网址的浏览器环境中。不要使用无痕模式。空间不足时会显示警告，请立即导出。
- 建议每周、更新前以及换手机前导出一次 JSON，并确认文件确实保存成功。可以把备份放到自己的 iCloud 云盘。删除网站数据、移除应用、更换正式域名或浏览器可能导致数据不可用。
- 同时开多个窗口时请只在一个窗口编辑，避免旧窗口覆盖新数据。
- 上传的新代码是公开网页；这里没有访问密码，但没有把你的个人数据写进发布包。知道网址的人只能打开空白应用，不能通过应用读取你手机里的记录。
- 应用没有广告、统计脚本和外部 CDN。托管平台仍可能记录页面访问日志。不要把备份放在公开网站目录中。
- 下次版本仍上传到同一个 Pages 项目的正式网址。替换全部应用文件时应递增 sw.js 的 VERSION；新缓存准备好后，应用顶部会提示“保存并更新”。更新不会主动清空本机数据。
- 如果误删数据，只能从你此前导出的备份恢复。没有服务器端数据恢复服务。

## 7. 文件与验证说明

这个包无需 npm 安装或编译，全部静态文件已就绪。主要文件是 index.html、styles.css、foods.js、core.js、app.js、sw.js、manifest.webmanifest、icons/ 和 Cloudflare 的 _headers。

已执行 JavaScript 语法、核心计算、旧版完整 JSON 迁移、备份往返、力量训练快照及文件包结构的自动化检查。没有把电脑检查冒充为 iPhone 真机验收；Safari 安装、实际下载分享和离线启动，请在部署后按上面的步骤确认。
